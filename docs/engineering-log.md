# 工程改进记录

用于记录有学习和简历表达价值的 Bug 修复、性能优化与架构调整。

## 2026-08-17｜建立可复现的 RKNN 模型性能对比

- 原先表现：现有单图入口每次只执行一帧且包含模型加载、图片处理和 YOLO 后处理，无法稳定比较不同输出维度 RKNN 模型的纯推理吞吐。
- 为什么改：ReLU 模型为 5 类、原版模型为 80 类，固定 80 类的后处理不能直接复用；反复重建 RKNN context 也会放大冷启动和动态频率波动。
- 具体做法：增加独立 `rknn_benchmark`，在同一 context 内预热后连续执行，统一计入 RKNN 输入设置、NPU 执行和输出获取，并报告平均值、P50、P95 和等效 FPS。
- 改进效果：RK3588 上各交替测试 5 轮、每轮预热 10 次并测量 100 次；原版平均 28.474 ms（35.12 FPS），ReLU 模型平均 18.863 ms（53.01 FPS），整模型吞吐提升 51.0%。由于类别数不同，该结果不能单独归因于激活函数替换。

## 2026-08-07｜实时检测流水线与并发队列重构

### 1. 统一管理实时处理流程

- 原先表现：`main()` 同时负责拉流、推理、画框、编码和推流，启动顺序分散，后续增加停止、重连和错误处理会越来越复杂。
- 为什么改：流程没有统一负责人，各模块的生命周期不清晰，错误发生后难以集中处理。
- 具体做法：新增 `VideoAnalyticsPipeline`，集中管理配置、媒体缓存、推理线程池及工作线程；入口程序只解析参数并启动 Pipeline。
- 改进效果：主流程更加清晰，后续可以在 Pipeline 中统一加入断线重连、状态管理和资源回收。当前需在 RK3588 开发板上完成运行验证。

### 2. 用阻塞队列替代循环查询

- 原先表现：队列为空时，线程每隔几毫秒醒来检查一次；既产生无效唤醒，也可能增加取帧延迟。
- 为什么改：循环 `sleep` 属于轮询等待，线程没有数据时仍会反复运行，而且程序停止时不容易唤醒等待线程。
- 具体做法：实现带互斥锁和条件变量的 `FrameQueue`。放入帧后立即通知消费者；队列关闭时唤醒所有等待线程。
- 改进效果：没有数据时线程会真正休眠，新帧到达后可立即唤醒；退出流程也有明确通知机制。CPU 和延迟改善待开发板实测。

### 3. 限制视频帧缓存长度

- 原先表现：处理速度低于输入速度时，视频帧可能持续积压，造成内存增长和画面延迟。
- 为什么改：实时监控更关注最新画面，长期处理旧帧会让结果失去实时性。
- 具体做法：为帧队列设置容量，默认保留 10 帧；超过容量时丢弃最旧帧。
- 改进效果：缓存占用有上限，系统过载时优先保持较新的画面，代价是高负载下可能丢帧。

### 4. 修复线程池共享容器的数据竞争

- 原先表现：一个线程在检查任务数量或查找结果时，另一个线程可能同时修改同一个 `queue/map`，运行结果不稳定。
- 为什么改：C++ 标准容器不支持无锁的并发读写，可能出现偶发崩溃、错误结果或难以复现的问题。
- 具体做法：任务队列和结果表分别使用互斥锁保护；使用条件变量等待“有任务”“有空位”和“有结果”；停止状态改为原子变量。
- 改进效果：共享数据的读写顺序明确，消除了已识别的数据竞争和忙等待。仍需结合开发板压力测试确认长期稳定性。

### 5. 分离推理和画框职责

- 原先表现：推理线程池内部直接绘制检测框，检测结果难以复用于告警、截图、统计或 JSON 上报。
- 为什么改：线程池应该只负责计算，结果如何使用应由业务层决定，否则每增加一种业务都要修改推理模块。
- 具体做法：新增 `InferenceResult`，统一返回帧 ID、图像和检测结果；画框移动到 Pipeline 的结果处理阶段。
- 改进效果：同一份检测结果可以继续扩展画框、告警、保存和上报逻辑，而不需要修改 YOLO 推理线程池。

### 当前限制

- MPP 解码、编码代码仍通过兼容接口绑定当前 `MediaBuffer`，尚不能安全地同时运行多路 Pipeline。
- 当前 Windows 环境缺少 CMake 和目标平台工具链；以上改动已完成静态检查，编译和性能数据待 RK3588 开发板验证。

## 2026-08-08｜增加 PC 端 YOLOv5 流程测试

- 原先表现：YOLO 检测代码只能依赖 RKNN 在 RK3588 上运行，没有开发板时无法快速检查输入、推理、画框和结果输出流程。
- 为什么改：开发早期的大部分流程问题不需要在板端排查；PC 上先验证可以缩短调试周期，并区分通用流程问题和 RK3588 平台问题。
- 具体做法：新增独立的 `pc_yolov5` 可选目标，使用 OpenCV DNN 加载 YOLOv5 ONNX，支持图片、视频和 PC 摄像头输入，并保存画框结果；CMake 可关闭全部 RK3588 目标，只构建 PC Demo。
- 改进效果：预期可在 PC 上验证完整的基础检测流程，同时不影响原有 RKNN/MPP 构建。当前环境缺少 CMake、OpenCV PC 开发包和 ONNX 测试模型，编译及检测结果待本机依赖准备完成后验证。

## 2026-08-08｜梳理项目结构和构建分组

- 原先表现：`CMakeLists.txt` 中重复列出媒体源文件和平台库，README 对源码分层的说明较粗，后续新增目标或移动目录时容易漏改。
- 为什么改：当前工程已经分出推理、媒体、Pipeline、PC Demo 等模块，但结构意图主要藏在文件名和 include 路径里，协作和扩展成本会逐渐升高。
- 具体做法：在 CMake 中增加源文件清单、公共 include 清单和平台库清单，减少重复目标配置；将各示例入口迁入 `apps/`，将 YOLOv5 模型封装和线程池迁入 `src/yolov5/`，将标签、测试素材和 RKNN 模型迁入 `assets/`；新增 `docs/project-structure.md` 记录当前结构和后续迁移顺序，并同步 README。
- 改进效果：入口、模型代码、媒体代码和运行资源的边界更清楚，`src` 不再混放 demo main 文件。当前 Windows 环境缺少 CMake，编译验证仍需在具备依赖的 PC 或 RK3588 开发板上完成。

## 2026-08-09｜兼容包含 Detect 解码的单输出 YOLOv5 模型

- 原先表现：RKNN C++ 后处理固定读取三个 INT8 检测头，而当前未裁剪的模型输出为单个 `[1, 25200, 85]` 张量，直接运行会访问不存在的第二、第三个输出。
- 为什么改：当前模型已经在图内完成网格和 anchor 解码，模型外只需置信度过滤、NMS 和坐标还原；同时后续仍计划尝试导出三个原始检测头，旧解码路径不能删除。
- 具体做法：保留原三检测头后处理，新增合并输出解码接口；`Yolov5` 根据输出数量自动选择单输出或三输出路径，并传递真实张量类型、量化零点和缩放系数。单输出兼容 `[1, N, 85]` 和 `[1, 85, N]` 排列。
- 改进效果：当前完整 YOLOv5 模型不再误用三头 anchor 解码，后续裁剪 Detect 节点后也能继续切回原后处理。合并输出核心逻辑已在 Ubuntu 使用合成预测数据完成编译和 NMS 冒烟测试；完整 RKNN 推理仍待 RK3588 开发板验证。
## 2026-08-10｜修复 YOLOv5 Detect 合并输出量化失真

- 原先表现：INT8 模型最终输出中的 objectness 和类别概率全部为零，验证集无检测结果。
- 为什么改：解码后坐标与概率合并量化，坐标动态范围主导量化尺度，导致概率信息丢失。
- 具体做法：新增独立导出脚本，在不修改 YOLOv5 源码的情况下临时替换内存中 Detect 的 forward，导出三路原始 Head；C++ 按 Head 尺寸匹配输出及其 scale/zp；修复排序后的置信度索引。
- 改进效果：三路 ONNX 和 FP/INT8 RKNN 均成功生成。进一步使用 500 张 COCO val2017 子集（970 个 person 框）复核，INT8 相对 FP 的 Recall 下降 0.0010、AP50 下降 0.0026，确认先前 28 张结果受小样本波动放大；原先 INT8 零检测问题已在 PC 模拟器修复，RK3588 性能与兼容性待真机验证。

## 2026-08-11｜RK3588 真机部署与运行可靠性收口

- 原先表现：板端系统 Runtime 不能加载当前 RKNN v6 模型；程序忽略参数、模型、推理和输出错误；RKNN 输出由应用直接 `free`；线程池全量构建缺少 pthread；媒体与基础目标无法独立构建和发布。
- 为什么改：这些问题会造成模型无法启动、错误结果仍返回成功、长时间运行内存管理风险，以及发布过程只能依赖开发者手工经验。
- 具体做法：使用项目私有 RKNN Runtime 1.5.3b6 并保留系统库；修复 RKNN 输出释放与模型缓冲释放；为图片和视频入口增加输入校验、退出码、错误阈值和汇总指标；分离流媒体构建选项，补齐 Threads 链接与安装 RPATH；增加版本化发布、SHA-256 校验、非 root systemd 服务和真实 NPU 启动前健康检查。
- 改进效果：RK3588 上全量基础目标编译通过；单图检测到 12 个目标，推理约 36～43 ms；三轮 1080p 视频共约 139 帧，推理错误和新增 NPU/IOMMU 错误均为 0。当前系统视频硬解/RGA 转换仍持续报错，端到端仅约 2.8 FPS，已列为摄像头上线阻断项，不将该路径标记为生产完成。

## 2026-08-11｜离线视频流水线从 16.54 FPS 优化到 78.17 FPS

- 原先表现：完整离线视频采用串行的“解码、预处理、推理、后处理、画框”流程，只达到 16.54 FPS；系统默认 `h264_rkmpp` 解码还会发生 RGA 错误，481 帧只交付 65 帧。
- 为什么改：NPU 单次模型执行约 24 ms，但解码和推理串行等待，三个 NPU 核心也没有通过多 context 形成帧级并行，硬解错误还会造成静默丢帧。
- 具体做法：直接在 640×640 模型尺寸内完成等比缩放和补边，减少大图中间缓冲；解码线程、1～3 个 RKNN context 和有序结果回收组成有界流水线；增加完整帧计数、单帧错误传播和显式软件 H.264 解码模式；画框成为可选且与推理并行的阶段。
- 改进效果：同一份 720p H.264、481 帧视频全部处理且错误为 0。1/2/3 context 分别达到 32.26/57.02/78.17 FPS；3 context 开启画框为 77.79 FPS，相对原可靠基线提升约 4.7 倍。满载后约 50°C，未发现新增 RKNPU/IOMMU 错误。
